# LayoutInfo 属性描述槽压缩 —— 背景

本文件存放理解本方案所需的背景知识与现有实现介绍。

## 1. LayoutInfo 是什么

`LayoutInfo`（`layout_info.h:30`，私有继承 `TaggedArray`）是 JSHClass 的属性布局数组：每个属性以「Key 槽 + Attr 槽」两个 8 B tagged 槽交织存放（`ELEMENTS_INDEX_LOG2` 交织编址，`layout_info-inl.h:40-90`）。Key 是属性名（堆引用），Attr 是 `PropertyAttributes`——本质 uint32 位域（writable/enumerable/configurable/fast-rep 等），经 `GetNormalTagged()` 以 tagged 值写入整 8 B 槽。

## 2. 规模：比 JSHClass 本体更大的开销

Top13 快照复算（`evidence/top13-layout-dedup-census.json`、`scripts/layout_dedup_census.py`，按去重目标节点统计，规避共享对象重复计数）：

| 指标 | 值 |
|---|---|
| 去重 Layout 数组 | 735,734 个（hclass 819,497 个，共享率仅 0.898，transition 机制天然共享接近上限） |
| Layout 总字节 | **109.25 MiB**（JSHClass 本体 68.78 MiB 的 1.59 倍） |
| 数据区（扣 16 B 头） | 98.02 MiB，其中 Attr 槽约 49 MiB |
| 尺寸公式核验 | 735,734/735,734 满足 `size == 16 + 16N`（N=属性数） |
| N 分布 | N=1: 75,144 / N=2: 34,040 / N=3: 80,527 / N=4: 150,711 / N=6: 30,535 / N=7: 90,574 / N=8: 75,319；平均 ≈8.8 |

## 3. Attr 槽的浪费结构

Attr 的有效信息 ≤32 bit，却按 tagged 值占满 8 B 槽（Smi 编码）。每属性 16 B 中 4 B 是纯浪费（50% 的 Attr 槽、25% 的整条目）。`TaggedArray` 体系已有原生字段先例（`JSNativePointer`、`InternalAccessor` 的 `ACCESSORS_FIXED_SIZE_FIELD`），raw u32 区属于既有模式内的改造。

## 4. 消费者边界（源码核验）

- 定义与访问器：`layout_info.h` / `layout_info-inl.h`（`GetKeyIndex`/`GetAttrIndex`/`GetAttr`/`SetAttr`）；
- 查找链：`js_hclass` 的 `LookupProperty*` 家族（fast/prototype/builtin/AOT/PGO 五入口）与 transition 追加属性路径；
- 枚举：`LayoutInfo::GetAllKeys/GetAllEnumKeys` 系列；
- AOT 编译的固定偏移读取（`LookupPropertyInAotHClass`）——内部编译契约，需版本 bump；
- GC：Attr 槽当前为 Smi 值（visitor 天然跳过），改 raw u32 后按 Key 区长度扫描，语义等价；
- 工具：serializer 案例表、heap snapshot/rawheap metadata、`js_metadata_test`。

## 5. 参考文档

- 数据基线与复算脚本：`../../evidence/top13-layout-dedup-census.json`、`../../scripts/layout_dedup_census.py`
- 同对象族方案（可叠加、版本契约建议同批）：`../jshclass-auxdata-sidecar/`
- 竞品对照：V8 `DescriptorArray`（details 与 key 分列、共享）；JSC `PropertyTable`
